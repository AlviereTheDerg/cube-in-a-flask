{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Python: Current File",
            "type": "shell",
            "command": "python",
            "args": ["-m", "${command:extension.commandvariable.file.relativeFileDotsNoExtension}"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}